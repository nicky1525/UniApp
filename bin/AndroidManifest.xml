<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.nic.uniapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/uniapp_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="it.nic.uniapp.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ListaEsami"></activity>
        <activity android:name="FunzioniEsami"></activity>
        <activity android:name="CalendarioEsami"></activity>
        <activity android:name="AddEsame"></activity>
        <activity android:name="UpdateEsame"></activity>
        <activity android:name="PopUpWindow"></activity>
    </application>

</manifest>